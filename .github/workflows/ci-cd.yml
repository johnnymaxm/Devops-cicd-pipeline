name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  ci-cd:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Instalar dependências
      run: npm install

    - name: Rodar testes
      run: npm test

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/devops:latest .

    - name: Login no Docker Hub
      run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

    - name: Push imagem
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops:latest

    # - name: Deploy via SSH
    #   uses: appleboy/ssh-action@v0.1.6
    #   with:
    #     host: ${{ secrets.SERVER_HOST }}
    #     username: ubuntu
    #     key: ${{ secrets.SSH_KEY }}
    #     script: |
    #       docker pull ${{ secrets.DOCKER_USERNAME }}/devops:latest
    #       docker stop app || true
    #       docker rm app || true
    #       docker run -d -p 3000:3000 --name app ${{ secrets.DOCKER_USERNAME }}/devops:latest
